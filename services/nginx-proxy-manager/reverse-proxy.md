# Nginx Proxy Manager
For convenience. Reverse proxy in conjunction with creating new local DNS domains (with Pi-hole) removes the need to type in the server's IP and remember a specific port for the service you're trying to access.  
Example: http://192.168.x.x -> pihole.home
